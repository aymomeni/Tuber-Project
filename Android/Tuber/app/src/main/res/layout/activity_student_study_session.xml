<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_student_study_session"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@layout/backrepeat"
    tools:context="cs4000.tuber.StudentStudySession">

    <RatingBar
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:id="@+id/rating_tutor_bar"
        android:numStars="5"
        android:theme="@style/RatingBar"/>

    <Button
        android:text="Submit"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/rate_tutor_button"
        android:textSize="18sp"
        android:layout_marginLeft="80dp"
        android:layout_marginRight="80dp"
        android:layout_below="@+id/rating_tutor_bar"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp"
        android:enabled="false" />

    <TextView
        android:text="@string/rating_text_student"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/rating_tutor_bar"
        android:layout_centerHorizontal="true"
        android:id="@+id/rate_tutor_text"
        android:textSize="24sp"
        android:textColor="@color/oil"
        android:textAlignment="center"
        android:layout_marginBottom="70dp" />
</RelativeLayout>
